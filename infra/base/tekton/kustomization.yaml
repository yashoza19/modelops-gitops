apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: tekton-pipelines

resources:
  - openshift-pipelines-operator.yaml
  - tekton-config.yaml
  - model-build-pipeline.yaml
  - model-build-tasks.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: gitops-llm-paper
      app.kubernetes.io/component: tekton-pipelines
