apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: openshift-pipelines-operator
  namespace: openshift-operators
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: openshift-pipelines-operator
    app.kubernetes.io/part-of: gitops-llm-paper
    app.kubernetes.io/component: operator-subscription
spec:
  channel: stable
  installPlanApproval: Automatic
  name: openshift-pipelines-operator-rh
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  # Pin to specific version to avoid drift
  startingCSV: openshift-pipelines-operator-rh.v1.15.1
---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-pipelines-operator
  namespace: openshift-operators
  annotations:
    argocd.argoproj.io/sync-wave: "29"
  labels:
    app.kubernetes.io/name: openshift-pipelines-operator-group
    app.kubernetes.io/part-of: gitops-llm-paper
    app.kubernetes.io/component: operator-group
spec:
  targetNamespaces: []
